# 更新日志 (Changelog)

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范。

## [0.1.4] - 2026-08-21

### 安全加固

- **RDP / WS 桥本机 SSRF 防护**：桥接令牌绑定目标地址；RDP 路径校验客户端 `destination` 与绑定目标一致，非 RDP 路径强制使用绑定目标（忽略 URL 中的 target 参数）；令牌增加 10 分钟过期与惰性剔除，防止本机其他进程利用有效令牌对内网任意 TCP 服务跳板
- **路径穿越防护**：导入解压 tar slip、ImportSessions 目标目录、密钥文件落盘、重命名同目录冲突、selectedPaths、DeleteFolder 防误删 `key/`、readLocal 防 TOCTOU 全面加固
- **主机指纹 TOFU 加固**：修复 `known_hosts.json` 临时保存 nil map panic；指纹不一致拒绝连接
- **会话文件写入原子化** + 临时文件权限收敛（文件 0644 / 目录 0755）
- **加密导入导出**：导入端重新加密失败 fail-close，仅解密成功才替换原值；`SshCopyKey` 公钥校验、`x509.EncryptPEMBlock` 废弃替换、`passphrase` 交互保护、`LocalRename` 防目录逃逸
- **SFTP** 远端写锁与大小限制、`CancelTransfer` 双关 panic 修复
- 全局 `killChildProcesses` 仅清理本项目子进程，不再误杀全机 node 进程

### 缺陷修复

- **SSH**：keepalive 计时器修复避免连接误判断开、readLoop goroutine 泄漏修复、closed 连接事件幂等 + 30s keepalive、stderr 独立读取防阻塞
- **Telnet / 串口**：协议状态机与错误连接处理；`SftpPanel` 编辑器脏关闭确认、高亮防抖、拖放目标 id 修正
- **终端**：`useXterm` / `useSessionAuth` 生命周期与 `onUnmounted` 修正、多行粘贴确认、选择即复制
- **RDP**：`active` 可见性切换与断开后重连入口、卸载清空 `rdpUI` 悬挂引用、X.224 协商响应 flags 兼容（保留位 0x00）、1MB 读限制解除与 relay 写超时、桥 HTTP server 超时与退出关闭、`bridgeWsURL` IPv6 QueryEscape
- **界面与交互**：`SettingsDialog` 透明度防抖、`ImportDialog` 校验与空路径提示、`ExportDialog` 重开清空密码、`SessionManager` / `AutoLogManager` 静默错误改为提示、`ScriptManager` 重命名后移动失效、`FileEditor` 自动保存竞态与卸载丢改
- **日志**：`AutoLogManager` 全量日志改为尾部读取（>50000 行），后端新增 `GetLogTail`

### 构建与测试

- 后端单测覆盖会话安全、RDP 桥、rdcleanpath 解析；前端 `vue-tsc` 类型检查通过
- 版本升至 0.1.4

## [0.1.3] - 2026-08-20

### 国际化

- 新增多语言支持：内置简体中文（默认）与英文，设置弹窗中可随时切换，语言选择持久化到配置文件
- 全部界面文案（组件、弹窗、菜单、右键菜单、状态提示）切换为 i18n 键值，无硬编码中文
- 语言切换即时生效：含 `t()` 的静态选项列表（SSH 算法分组、校验位、密钥类型、右键菜单等）随语言切换实时更新
- 架构可扩展：语言文件位于 `frontend/src/i18n/locales/`，新增语言仅需添加对应 locale 文件

## [0.1.2] - 2026-08-20

### 新增：RDP 图形会话

- 新增 RDP 会话类型，支持远程桌面图形连接：后端 RdpService 统一管理连接与 token 鉴权，WebSocket 桥接层（wsbridge）转发 RDP 协议数据
- 前端新增远程桌面标签页（基于 IronRDP 的 iron-remote-desktop-rdp）：全屏画面渲染、鼠标/键盘交互、连接状态指示与错误提示
- 标签页生命周期与既有标签管理器深度集成：拖动排序/分屏/关闭与连接 token 释放联动

### 渲染与显示优化

- 自定义构建 IronRDP：脏区域缓冲 + rAF 合并绘制，每帧仅一次 putImageData，消除网络卡顿时的画面撕裂
- 显示比例固定为服务器原生分辨率（物理 1:1，按设备像素比缩放适配），黑边保留、不拉伸；窗口尺寸变化不再触发分辨率重新协商（兼容 xrdp 0.9 的 deactivate-reactivate 限制）
- 修复拖动标签页重建组件导致的 RDP 连接断开（token 释放时机改为标签页真正关闭时）

### 依赖

- `@devolutions/iron-remote-desktop-rdp` 切换为自定义构建发布包（GitHub Release 分发），用户安装即用，无需自行编译 Rust/wasm

## [0.1.1] - 2026-08-19

### 主题

- 原生标题栏跟随应用亮暗主题
- 面板透明度默认改为 100%

### 构建与发布

- 更新应用图标源并重新生成全平台图标
- MSIX 包标识名改为发布者前缀格式 `dingtongbin.AceShell`
- 修复 MSIX 版本替换正则误伤 MinVersion，发布者显示名对齐商店账号
- 拆分 MSIX 上传为独立发布步骤（修复 `&&`/`||` 表达式求值为布尔的问题）
- Create Release 步骤仅 tag 触发时执行，手动触发工作流为构建预演

## [0.1.0] - 2026-08-18

首个功能完整版本，以下为当前版本已实现的功能全景。

### 会话管理

- 支持 SSH / Telnet / 串口 / SFTP / HTTP 五类会话，统一在资源管理器中以树形组织
- 会话文件使用 TOML 存储，监听文件系统变化自动刷新树，无需手动轮询
- 树形交互：双击文件夹展开/收纳（仅展开一层、收纳递归折叠）、拖拽移动（目标冲突时弹窗改名）、内联重命名、递归删除二次确认、元信息查看
- 新建/编辑会话弹窗：左侧类型选择（SSH/Telnet/串口），右侧按类型切换表单；共享字段仅会话名与 IP；保存仅写入当前类型字段，自动维护创建/更新时间
- SSH 连接流程：主机指纹 TOFU 校验（首次连接确认指纹并写入文件夹级指纹文件与全局索引、指纹不一致拒绝连接）、密码登录与密钥登录、凭证记住、密码错误自动重试（上限 5 次）
- 会话密码、密钥口令等敏感字段经认证加密落盘，不出现明文
- 串口参数（端口/波特率/数据位/停止位/校验位）持久化并可一键重连

### 标签页

- 外层标签页管理器统一管理所有连接，每个连接一个标签页
- 标签页状态圆圈四色：绿=连接成功、黄=异常断开、红=连接失败、白=超时断开
- 标签页数量超出时两侧出现滑动按钮，鼠标悬浮标签页时滚轮横向滚动
- 拖拽排序、拖拽分屏（上/下/左/右/中心交换，四象限高亮提示、插入指示线、分隔条可拖比例）、拖拽出窗口
- 标签页右键菜单：新建连接、导出会话、在新窗口打开等；关闭标签页支持会话级"不再提醒"记忆与全局开关（串口等无会话文件标签页跟随全局设置，弹窗内以小字说明）
- 支持水平/垂直两种标签方向，垂直模式可拖拽调整宽度
- 暗色模式下活动标签页背景与终端背景统一为深灰，无边框、上圆角

### 终端渲染与交互

- 基于 xterm.js 的现代终端渲染：真色彩、TUI 全屏程序、超链接识别、emoji 与宽字符正确布局、滚动回滚缓冲（可配置行数）
- 鼠标交互：左键拖动选中即复制、右键粘贴、单击定位、双击选词、三击选行
- 多行粘贴弹出 CodeMirror 确认窗，可编辑后按行发送
- 滚动条按内容比例显示、可拖动，滚轮翻页
- 终端个性化设置：字体与字号、字体颜色、背景色、背景图、背景透明度、光标样式（竖线/方块/横线）与闪烁、选中即复制开关
- 终端工具栏：复制、粘贴、SFTP、执行脚本、导出日志、清除回滚、清屏
- 终端默认纯白字体深灰背景，字体统一 Cascadia Code 等宽字体

### 状态栏

- 底部状态栏显示当前活动标签页信息：
  - 未打开标签页时仅显示"未连接"
  - SSH 显示 `SSH:用户@地址:端口`，Telnet 显示 `Telnet:地址:端口`，串口显示 `串口:设备:波特率`
  - 文件编辑标签页显示文件类型（如 json、sh）
  - 右侧显示编码（utf-8，无编码不显示）与光标行列（文件编辑器中为真实光标位置）

### SFTP

- 双栏布局：本地与远端目录并排，各自独立导航与刷新
- 浏览：进入目录、返回上级、刷新、文件大小/修改时间/权限展示
- 传输：文件与文件夹上传下载、断点续传、进度展示与取消；支持系统文件直接拖拽上传、拖拽远端文件下载到本地
- 大文件传输启用并发写，避免卡顿
- 应用内在线编辑：双击远端文本文件在应用内打开编辑器（仅文本白名单、后端 UTF-8 校验、行号显示），保存回传远端
- 右键菜单：本地与远端文件/文件夹的新建、重命名、复制路径、删除（本地进回收站）、外部程序打开/编辑（仅文本文件）
- 本地目录默认用户主目录
- 懒加载：仅活动且面板可见时才发起目录请求，不轮询

### 脚本与文件编辑

- 脚本管理器与会话管理器同风格树形展示，识别任意文件类型，支持搜索、展开收纳、拖拽移动、重命名、删除确认
- 双击文件在标签页中打开编辑器：等宽字体、行号列、highlight.js 语法高亮、Tab 缩进
- 同一文件仅保留一个编辑标签页，重复打开自动定位并激活已有标签页
- 标签页存活期间保留光标位置与阅读滚动位置，切换回活动时恢复焦点
- 保存：Ctrl+S 手动保存；设置中可开关自动保存（固定 5 秒间隔，持续输入不失效）
- 标签页标题状态指示：黄色圆点=未保存、绿色对勾=已保存；未保存关闭时弹窗询问（取消/不保存/保存并关闭）
- 新建脚本弹窗：预填"新建脚本.sh"，按输入原样创建不自动补后缀，重名提示
- 新建文件夹、导入导出流程与会话树一致

### 日志

- 自动记录所有连接的输入与输出到纯文本日志文件，命名 `地址 - 时间`（秒级）
- 会话级日志开关优先于全局开关，都未开启则不记录
- 日志管理器按树形浏览历史日志，查看器使用等宽字体与行号、固定弹窗尺寸

### 导入导出

- 导出：树形多选（文件夹递归勾选、父级半选联动）→ 设置导出密码（8~64 位、须含大写/小写/数字/符号至少三类）→ 选择保存路径 → 生成加密导出包（.as9，认证加密）
- 导出包可携带全局密钥；导出时先解密会话内敏感字段再打包，包整体用导出密码加密
- 导入：单对话框流程——选择文件（可拖拽）→ 输入密码点击"确定"解析 → 左侧包内文件树多选（文件夹与会话文件）→ 右侧本地文件夹树单选目标（sessions 根为顶层）→ 选择重复处理策略（跳过/覆盖）→ 点击导入
- 二次确认弹窗：重排展示选中的包内内容（含选中文件夹下的会话文件）与导入目标文件夹，确认后执行
- 导入期间锁定 sessions 目录防止并发读写，完成后释放；崩溃残留锁自动清理
- 导入仅落盘选中内容，会话敏感字段以本机密钥体系重新加密后写入
- 解析阶段仅读取包内文件树元数据，不长期保留解密内容于内存

### 设置与外观

- 设置弹窗左侧分类导航：通用 / 视图 / 终端 / 文件编辑 / 标签 / 关于
- 通用：主题模式（深色/浅色/跟随系统）、面板透明度、壁纸
- 视图：串口管理器显隐、帮助显隐、GitHub 按钮显隐
- 终端：终端个性化（字体/颜色/光标/背景）、工具栏开关、滚动缓冲行数
- 文件编辑：自动保存开关
- 标签：标签方向（水平/垂直）、关闭标签页不弹确认
- 所有即时设置写入 config.toml 持久化，变更即时生效

### 界面布局

- 顶部菜单栏：文件 / 编辑 / 工具 / 帮助 下拉菜单 + 右侧资源管理器/本地终端/设置按钮
- 左侧工具栏：资源管理器开关、GitHub（打开项目主页）、帮助、设置
- 资源管理器面板：标题栏 + 会话/脚本/日志 二级标签，宽度可拖拽调整并持久化
- 主区域：外层标签页管理器；底部状态栏
- 帮助弹窗：功能说明、项目主页与博客链接，内容区滚动条包裹、尺寸固定
- 关于页面：应用信息、技术栈、项目主页、博客、GPL-3.0 版权许可声明

### 安全

- 主密钥加密体系：会话密码、密钥口令等敏感字段认证加密落盘，内存短存
- SSH 主机指纹 TOFU：文件夹级指纹文件 + 全局索引表双重维护
- 导入导出口令保护、导入内容本机重加密
- 路径遍历防护、单实例运行锁

### 其他

- 版本号由后端单一来源提供（GetVersion），构建时可注入覆盖，界面不硬编码
- 剪贴板服务、HTTP 会话浏览器扫描与选择
